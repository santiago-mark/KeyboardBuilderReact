export const CurrentBuild = (
    state = {
        chosenKeyboardCase: '',
        chosenKeycapSet: '',
        chosenSwitches: '',
        chosenStabilizers: ''
    }, action) => {
    switch (action.type) {
        default:
            return state;
    }
};